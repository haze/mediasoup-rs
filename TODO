DOCUMENT EVERYTHING
Client:
join (done)
leave (done)
sync (wip)

media transports
from file:
video file:
audio file:

from system

magic variable URLs

use rust::Str over rust::String


More ugly code cleanup:
In the c++ code, everything is in one flat namespace (including box's to callbacks for update things)
Separate these out into individual structs so I don't pollyute the namespace
